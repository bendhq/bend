import type { FastifyInstance, FastifyPluginCallback } from "fastify";

const health: FastifyPluginCallback = (app: FastifyInstance, _opts, done) => {
  app.get("/health", async () => ({ ok: true }));
  done();
};

export default health;
