import { Router } from "express";

const router = Router();

router.get("/", (_req, res) => {
  res.json({
    name: "<%= name %>",
    about: "Bend - modern backend project generator and bundler.",
    more: "https://bendhq.org"
  });
});

export default router;
