{
  "watch": ["src"],
  "ext": "<%= language === 'ts' ? 'ts,json' : 'js,json' %>",
  "ignore": ["dist", "node_modules"],
  "exec": "<%= language === 'ts' ? 'ts-node src/server.ts' : 'node src/server.js' %>",
  "env": {
    "NODE_ENV": "development"
  }
}
