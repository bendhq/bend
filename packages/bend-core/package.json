{
  "name": "bend-core",
  "version": "1.0.0",
  "description": "Bend core - template engine, renderer, and templates",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "dist/templates-compiled", "src/templates", "public"],
  "scripts": {
    "build": "tsup src/index.ts src/cli/index.ts --dts --format cjs,esm --out-dir dist && node ../../scripts/compile-templates.mjs",
    "test": "vitest",
    "lint": "eslint src --ext .ts,.js"
  },
  "dependencies": {
    "ejs": "^3.1.10"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  }
}
