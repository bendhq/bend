# <%= name %>

Generated with **Bend** - the modern backend project generator and bundler.

---

## 🚀 Getting Started

### 1. Install dependencies
<%= pkgm %> install

### 2. Start development server
<%= pkgm %> run dev

### 3. Build for production
<%= pkgm %> run build

### 4. Run the compiled app
<%= pkgm %> start

---

## 🧩 Stack

| Option | Selection |
|---------|------------|
| **Language** | <%= language === 'typescript' ? 'TypeScript' : 'JavaScript' %> |
| **Framework** | <%= framework.charAt(0).toUpperCase() + framework.slice(1) %> |
| **ORM** | <%= orm.charAt(0).toUpperCase() + orm.slice(1) %> |

---

## ⚙️ Environment Variables

Create a `.env` file based on the example provided:

cp .env.example .env

Then update the variables as needed.

---

## 📂 Project Structure

<%= name %>/
├─ src/
│  ├─ server.<%= language === 'typescript' ? 'ts' : 'js' %>
│  ├─ routes/
│  ├─ middlewares/
│  └─ db/
├─ .env.example
├─ package.json
├─ tsconfig.json
└─ README.md

---

## 🧠 About Bend

Bend helps you scaffold backend apps instantly with a preconfigured, production-ready setup.  
It supports **JavaScript or TypeScript**, **Express or Fastify**, and **Mongoose or Prisma** — all with built-in **hot reload**, **esbuild bundling**, and smart defaults.

For more info, visit https://github.com/bendhq/bend
