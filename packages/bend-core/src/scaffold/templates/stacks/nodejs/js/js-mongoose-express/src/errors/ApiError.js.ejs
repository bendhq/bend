export default class ApiError extends Error {
  constructor(statusCode = 500, message = 'Internal Error', extras = {}) {
    super(message);
    this.statusCode = statusCode;
    this.extras = extras;
    Error.captureStackTrace(this, this.constructor);
  }
}
